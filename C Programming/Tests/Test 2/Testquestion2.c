#include<stdio.h>
void main()
{
	int no;
	 printf("Enter any number");
	 scanf("%d",&no);
	 
	if(no>0)
	 printf("Number is positve");
	 
	else if(no<0)
	 printf("Number is negative");
	 
	else
	  printf("Number is zero");   
}